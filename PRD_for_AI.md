# 旅途 (TravelAssistant) - 核心需求与领域规范文档

## 1. 业务愿景与系统边界
*   **产品定位**：一个高可用、基于多智能体（Multi-Agent）编排的端到端旅游规划 API 服务。
*   **交互形式**：纯 API 接口交互（无 GUI 渲染需求），接收自然语言诉求，输出结构化 JSON 行程计划。
*   **核心约束**：绝对禁止大模型产生“地点幻觉”。所有地点、天气、酒店信息必须来源于高德地图 API，如果 API 故障，必须走系统预设的兜底策略，宁可返回极简的真实数据，也不允许 AI 凭空捏造。

## 2. 领域实体模型 (Domain Entities)
> 要求：所有内部流转数据及最终输出必须严格反序列化为以下结构。

*   **Location (位置)**：包含 `名称 (name)`、`高德adcode`、`经度 (lng)`、`纬度 (lat)`。
*   **POI (景点/兴趣点)**：继承自 Location，附加 `建议游玩时长 (duration)`、`门票预估 (price)`。
*   **Hotel (食宿)**：继承自 Location，附加 `距离核心景点距离 (distance)`。
*   **Weather (气象)**：包含 `日期 (date)`、`天气状况 (condition)`、`气温区间 (temperature)`。
*   **Itinerary (单日行程)**：包含 `日期`、`气象信息`、`当日酒店`、`游玩景点列表 (有序)`。

## 3. 多智能体角色职责 (Agent Roles)
本系统由一个 Supervisor 和三个子 Agent 组成，各司其职：

*   **Supervisor (行程总监)**：负责意图识别、提取出游城市和天数、调度子 Agent、最终将子 Agent 的碎片信息编排为天级别的 `Itinerary` 列表。
*   **WeatherAgent (气象专员)**：只接收城市名，调用高德天气 API，输出对应天数的逐日天气。
*   **POIAgent (景点专员)**：接收城市名和用户偏好，调用高德关键字搜索 API，输出必去景点的经纬度列表。
*   **HotelAgent (食宿专员)**：**必须依赖 POIAgent 的结果**。接收 POI 的经纬度，调用高德周边搜索 API，筛选出符合逻辑的酒店和餐饮。

## 4. 核心工作流与并发拓扑 (Workflow)
必须基于 LangGraph 状态图实现以下非对称 DAG（有向无环图）流转：

1.  **START** -> Supervisor 提取核心参数（城市、天数）。
2.  **Fan-out (并行化)** -> 同时触发 WeatherAgent 与 POIAgent，异步收集基础信息。
3.  **Sync (同步点)** -> 必须等待 POIAgent 返回并确定核心经纬度后，更新全局 State。
4.  **Chain (串行依赖)** -> 触发 HotelAgent，基于全局 State 中的经纬度就近搜索。
5.  **Fan-in (聚合)** -> Supervisor 汇总 State 中所有真实数据，执行最终的排版和结构化输出。

## 5. 容错与降级 SLA (Service Level Agreement)
> 必须在代码中实现严格的 4 级降级策略，保障服务 100% 可用：

*   **Level 1 (API 防御)**：若高德地图接口 HTTP 超时（>5s）或触发限流，底层 Tool 立即捕获异常，返回 `[]` (空列表)，切勿抛出异常导致进程崩溃。
*   **Level 2 (筛选容错)**：若 LLM 多次（>3次）无法根据乱序 POI 编排成合理路线，直接丢弃 LLM 结果，使用按距离排序的贪心算法兜底。
*   **Level 3 (编排熔断)**：若 Supervisor 节点超时假死，中断当前 Graph，走本地硬编码的“经典路线 JSON 库”返回该城市的默认行程。
*   **Level 4 (全局兜底)**：若发生不可知严重 Panic，全局拦截，返回默认的兜底极简文案：“系统正在维护，建议您在 [当前城市] 核心商圈自由探索。”